{
  "pluginAlias": "LedStrip",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "LED Strip"
      },
      "uuid": {
        "title": "BLE Device UUID",
        "type": "string",
        "required": true,
        "placeholder": "be320202f8e8",
        "description": "Find via `hcitool lescan`, lowercase, no colons."
      }
    }
  }
}